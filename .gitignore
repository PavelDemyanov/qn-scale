# Личные данные: журнал взвешиваний и дампы эфира (там видны все домашние устройства)
measurements.csv
scan_log.jsonl
*_out.txt

# Окружение и обёртка вокруг интерпретатора (собирается скриптом)
.venv/
BLEScan.app/

# Xcode
ios/Libra.xcodeproj/
ios/build/
build/
DerivedData/
*.xcuserdatad
*.xcuserstate

.DS_Store
__pycache__/
ios/build-sim/
# Каталоги сборки, архива и экспорта: там подписанный ipa, dSYM'ы и логи Xcode
# с сертификатом разработчика — в публичном репозитории им не место.
ios/build-archive/
ios/build-export/
ios/build-shots/
ios/ExportOptions.plist
