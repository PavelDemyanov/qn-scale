# Протокол весов Libra CS20C1 (QN / Qingniu / Yolanda)

Разобрано вживую 2026-08-12 перехватом BLE на маке. Родное приложение — Feelfit.

## Как весы видно в эфире

| Что | Значение |
|---|---|
| Имя в рекламе | `QN-Scale` (появляется не в каждом пакете) |
| **Рекламируемый** сервис | `FFE0` |
| **Реальный** сервис в GATT | `FFF0` |
| Notify (данные от весов) | `FFF1` |
| Write (команды к весам) | `FFF2`, запись **с подтверждением** |
| Manufacturer data | `0465 01 xx xx` + MAC задом наперёд |
| Батарея | сервис `180F`, характеристика `2A19` |

Две ловушки, на которых легко застрять:

1. **Реклама врёт про сервис.** Сканировать надо по `FFE0`, а характеристики брать
   из `FFF0`. Драйвер openScale поддерживает оба варианта (тип 1 — `FFE0/FFE1/FFE3`,
   тип 2 — `FFF0/FFF1/FFF2`); CS20C1 — второй тип.
2. **Запись только `withResponse`.** Характеристика `FFF2` объявлена как `write`
   (без `write-without-response`), и записи «без ответа» весы молча игнорируют —
   выглядит так, будто рукопожатие не проходит.

Весы вещают только когда проснулись (на них наступили) и засыпают через несколько
секунд. Подключение к «уже засыпающим» весам даёт живую сессию без единого измерения.

## Формат кадра

```
[опкод][длина][версия протокола][...данные...][контрольная сумма]
```

Контрольная сумма — сумма всех предыдущих байтов по модулю 256.
Исключение: кадр времени `0x02` идёт без суммы.

## Обмен целиком

```
весы → 0x12   привет: MAC, версия протокола (0x15)
мы   → 0x13   единицы измерения (0x01 = кг)
мы   → 0x02   текущее время
весы → 0x14   настройки приняты
мы   → 0x20   синхронизация времени
весы → 0x21   служебный кадр (можно игнорировать)
весы → 0x10   измерения, поток ~2 раза в секунду
```

Без синхронизации времени (`0x02` и `0x20`) поток `0x10` **не начинается** — весы
отвечают на `0x13`, подтверждают настройки и замолкают.

### Кадры от весов

`0x12` — представление (MAC здесь вымышленный, `aa:bb:cc:11:22:33`):
```
12 0f 15 33 22 11 cc bb aa 31 05 31 00 05 39
      │  └────── MAC задом наперёд ──────┘        байт 10 = 0x05
      версия
```
Байт 10 в openScale означает делитель веса (`1` → /100, иначе /10), но на CS20C1
там `0x05`, а вес всё равно приходит в **сотых долях килограмма**. Делитель
надёжнее выбирать по правдоподобности результата.

`0x10` — измерение:
```
10 0b 15 1f ae 01 02 15 01 e4 00 fa
         └─┬──┘ │  └──┬──┘ └──┬──┘  └ сумма
      вес BE   стаб.  имп.1   имп.2
   0x1FAE = 8110 → 81.10 кг
```
* байты 3–4 — вес, big-endian, сотые доли кг
* байт 5 — `0x01` вес зафиксирован, `0x00` ещё меняется
* байты 6–7 и 8–9 — импеданс в омах (две пары электродов), BE

`0x23` — измерение из памяти весов (ответ на запрос `0x22`): вес в байтах 10–11 BE /100,
импеданс в 13–14 и 15–16, little-endian.

### Команды к весам

| Команда | Байты |
|---|---|
| Единицы = кг | `13 09 <вер> 01 10 00 00 00 <сумма>` |
| Текущее время | `02 <4 байта времени LE>` (без суммы) |
| Синхронизация времени | `20 08 <вер> <4 байта времени LE> <сумма>` |
| Запрос истории | `22 06 <вер> 00 03 <сумма>` |

Время — секунды от 2000-01-01, little-endian, смещение `946702800`.

## Проверка

| Экран весов | Сырое значение | Расшифровка |
|---|---|---|
| 81.10 кг | `0x1FAE` = 8110 | 8110 / 100 |
| 81.20 кг | `0x1FB8` = 8120 | 8120 / 100 |

## Источники

Опорный разбор — драйвер `QNHandler.kt` из [openScale](https://github.com/oliexdev/openScale)
и ветка про QN-Scale на [форуме OpenMQTTGateway](https://community.openmqttgateway.com/t/integrate-qn-scale-with-bluetooth-ble-help-wanted/1586).
Всё, что выше, перепроверено на живых весах.
